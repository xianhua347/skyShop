server:
  port: 8080
spring:
  application:
    name: pet-visits-service
  datasource:
    url: jdbc:mysql://localhost:3306/sky_shop?serverTimezone=GMT%2B8&useSSL=false&&useUnicode=true&&CharacterEncoding=utf-8
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

springdoc:
  swagger-ui:
    path: /api.html
logging:
  config: classpath:logback-spring.xml